apiVersion: v1
kind: Service
metadata:
  name: {{ include "predictmaint.fullname" . }}
  labels:
    {{- include "predictmaint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "predictmaint.selectorLabels" . | nindent 4 }}
