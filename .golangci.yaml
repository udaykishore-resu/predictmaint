version: "2"
run:
  timeout: 5m
  tests: true
linters:
  default: standard
  enable:
    - bodyclose
    - copyloopvar
    - errorlint
    - gocritic
    - gosec
    - misspell
    - nolintlint
    - prealloc
    - revive
    - unconvert
    - unparam
  settings:
    gosec:
      excludes:
        - G404 # math/rand is intentional for deterministic detectors and the simulator
    revive:
      rules:
        - name: exported
          disabled: true
  exclusions:
    rules:
      - path: _test\.go
        # bodyclose: the test HTTP helper reads and closes the body before
        # returning *http.Response; the linter cannot see through the helper.
        linters: [gosec, errorlint, unparam, bodyclose]
formatters:
  enable:
    - gofmt
    - goimports
